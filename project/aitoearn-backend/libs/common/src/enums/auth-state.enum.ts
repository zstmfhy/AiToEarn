export enum ChannelAuthTaskStatus {
  Pending = 0,
  Completed = 1,
}

export enum ChannelAuthSessionStatus {
  Pending = 'pending',
  Completed = 'completed',
  Failed = 'failed',
}

export enum VerificationCodeStatus {
  Pending = 0,
}
