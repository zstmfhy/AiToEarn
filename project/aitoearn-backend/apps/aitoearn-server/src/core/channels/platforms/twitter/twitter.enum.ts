export enum TwitterReplySettings {
  Following = 'following',
  MentionedUsers = 'mentionedUsers',
  Subscribers = 'subscribers',
  Verified = 'verified',
}

export enum TwitterMediaProcessingState {
  Pending = 'pending',
  InProgress = 'in_progress',
  Succeeded = 'succeeded',
  Failed = 'failed',
}

export enum TwitterPostMediaType {
  Photo = 'photo',
  Video = 'video',
  AnimatedGif = 'animated_gif',
}
